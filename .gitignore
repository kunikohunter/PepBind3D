__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store

# Structure data belongs in the deposited dataset, not in git
*.pdb
*.cif
*.silent
*.silent.bak
*.sc

# Rosetta run trees: tens of GB, regenerable from the scripts
benchmark/rosetta_arm/arm/
benchmark/rosetta_arm/pilot/
benchmark/refs/cif/
benchmark/af2_msa/msa_cache/
benchmark/wrappers/test_*/
hlac_production/
hlac/
run/

# AlphaFold2 and model intermediates
*.pkl
*.npz
msas/
features.pkl
result_*.pkl
ranked_*.pdb

# Cluster logs and job artifacts
logs/
*.out
*.err
*.log
slurm-*.out
ROSETTA_CRASH.log
# except the regeneration run records, kept as provenance
!regeneration/*.out

# Job lists rebuilt by build_arm.py
arm_joblist.txt
arm_skipped.csv
dock_tasklist.txt

# Per-prediction fold output from analysis/affinity_baseline.py: 88k rows,
# ~15 MB, regenerable. The reported numbers are in
# affinity_baseline_metrics.json and affinity_baseline_per_allele.csv.
analysis/results/affinity_baseline_fold_predictions.csv

# Manuscript and editorial material. The scripts that build the manuscript
# edits are code and stay; the drafts, letters and plan are not published here.
*.docx
revisions/*
!revisions/*.py

# Local working files
notes/
scratchpad/
.loops-mem/
.claude/
CLAUDE.md

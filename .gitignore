__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store

# Structure data belongs in the deposited dataset, not in git
*.pdb
*.cif
*.silent
*.silent.bak
*.sc

# Rosetta run trees: tens of GB, regenerable from the scripts
hlac_production/
hlac/
run/

# Cluster logs and job artifacts
logs/
*.out
*.err
*.log
slurm-*.out
ROSETTA_CRASH.log
# except the regeneration run records, kept as provenance
!regeneration/*.out

# Job lists rebuilt by the docking scripts
dock_tasklist.txt

# Per-prediction fold output from analysis/affinity_baseline.py: 88k rows,
# ~15 MB, regenerable. The reported numbers are in
# affinity_baseline_metrics.json and affinity_baseline_per_allele.csv.
analysis/results/affinity_baseline_fold_predictions.csv

# Manuscript and editorial material: drafts, letters, the plan, and the scripts
# that build the tracked-changes files. None of it is published here -- the
# edit scripts carry manuscript prose in their find/replace rules, so they are
# editorial material too, not code.
*.docx
revisions/*

# Local working files
notes/
scratchpad/
.loops-mem/
.claude/
CLAUDE.md
